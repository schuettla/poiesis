# Moved — see `docs/POIESIS_PLAN.md`

This plan (Phase 10: memory, context, recall, soul, grammar, loop hygiene)
was absorbed into the **Poiesis master plan** on 2026-07-18, when the project
was renamed Nexus → Poiesis and extended with the autopoietic layer
(reflection/lessons, self-healing, recipes, autonomy ladder, organism UI).

Nothing was dropped: Parts III of `POIESIS_PLAN.md` contains the full former
content of this document, adapted (`soul_proposals` → `change_proposals`,
`tool_stats` gained `conversation_id`, `memory_fts` gained `kind`, layout
gained `lessons/` `recipes/` `.quarantine/`). Do not implement from this
file.
